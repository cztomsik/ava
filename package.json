{
  "private": true,
  "name": "ava",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "postinstall": "git submodule update --init --recursive",
    "build": "esbuild ./src/app/main.tsx --bundle --outdir=./src/app/dist",
    "watch": "npm run build -- --sourcemap --watch",
    "check": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "prettier": {
    "semi": false,
    "arrowParens": "avoid",
    "printWidth": 120
  },
  "dependencies": {
    "@preact/signals": "^1.1.5",
    "bootstrap": "^5.3.1",
    "goober": "^2.1.13",
    "markdown-it": "^13.0.1",
    "preact": "^10.16.0",
    "preact-router": "^4.1.2"
  },
  "devDependencies": {
    "esbuild": "^0.18.19",
    "typescript": "^5.0.2"
  }
}
