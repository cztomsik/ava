{
  "private": true,
  "name": "ava",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "app.js",
  "scripts": {
    "postinstall": "git submodule update --init --recursive",
    "build-llama.cpp": "cd llama.cpp && make clean && make LLAMA_METAL=1 main server",
    "build": "npm run build-llama.cpp && vite build",
    "dev": "vite",
    "preview": "npm run build-llama.cpp && vite preview",
    "check": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "prettier": {
    "semi": false,
    "arrowParens": "avoid",
    "printWidth": 120
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "@preact/signals": "^1.1.5",
    "bootstrap": "^5.3.1",
    "history": "^5.3.0",
    "markdown-it": "^13.0.1",
    "preact": "^10.16.0",
    "preact-router": "^4.1.2"
  }
}
